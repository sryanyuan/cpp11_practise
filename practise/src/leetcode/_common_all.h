#ifndef _INC_COMMON_ALL_
#define _INC_COMMON_ALL_

#include <string>
using std::string;
#include <vector>
using std::vector;
#include <list>
using std::list;
#include <unordered_map>
using std::unordered_map;
#include <stack>
using std::stack;
#include <algorithm>
#include <stdlib.h>
#include <unordered_set>
using std::unordered_set;
#include <sstream>
#include <stdlib.h>
#include <bitset>
using std::bitset;

#ifndef max
//#define max(a,b) (((a) > (b)) ? (a) : (b))
#endif

#endif
